// Preload script
window.addEventListener('DOMContentLoaded', () => {
    // You can expose additional APIs to the renderer process here if needed
    // For example, you could add custom functions or access to Node.js modules
  });